<%= form_for(product) do |f| %>
  <% if product.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(product.errors.count, "error") %> prohibited this product from being saved:</h2>

      <ul>
      <% product.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end #if %>

  <div class="field">
    <%= f.label :Name %>
    <%= f.text_field :name %>
  </div>

  <div class="field">
    <%= f.label :Beschreibung %>
    <%= f.text_area :description %>
  </div>

  <div class="field">
    <%= f.label :Herausgabedatum %>
    <%= f.date_select :release_date %>
  </div>

  <div class="field">
    <%= f.label "Verkauft" %>
    <%= f.check_box :on_sale %>
  </div>

  <div class="field">
    <%= f.label "Unverkäuflich" %>
    <%= f.check_box :not_for_sale %>
  </div>
  <div class="field">
    <%= f.label :Preis %>
    <%= f.text_field :price %>
  </div>

  <div class="field">
    <%= f.label :Kategorie %>
    <%= f.collection_select(:product_category_id, ProductCategory.all, :id, :name)  %>
  </div>

  <div class="field">
    <%= f.label :Bild %>
    <%= f.file_field :image %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
