<%title "Alle Produkte"%>
<p id="notice"><%= notice %></p>


    <% @products.each do |product| %>
          <div class="box col-3 col-m-6">
            <div class="boxcontent"> 
                <%= link_to(image_tag(@product.image(:medium)), product) %>
            </div>
          </div> 
    <% end %>



